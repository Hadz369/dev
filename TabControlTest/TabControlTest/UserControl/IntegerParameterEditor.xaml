<UserControl x:Class="TabControlTest.IntegerParameterEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
    <Grid HorizontalAlignment="Left">
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="80"  Width="{Binding LabelWidth}"/>
            <ColumnDefinition MinWidth="120" Width="{Binding EditorWidth}" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" HorizontalContentAlignment="Left" VerticalContentAlignment="Top" Padding="5,3" Content="{Binding Label}" />
        <TextBox Grid.Column="1" Text="{Binding Value}" />
    </Grid>
</UserControl>
